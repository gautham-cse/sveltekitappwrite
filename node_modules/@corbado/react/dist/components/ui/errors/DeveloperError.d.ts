import type { CorbadoError } from '@corbado/web-core';
import React from 'react';
type Props = {
    error: CorbadoError;
};
export declare const DeveloperError: ({ error }: Props) => React.JSX.Element;
export {};
