import type { CSSProperties } from 'react';
import React from 'react';
export interface IconProps {
    onClick?: () => void;
    className?: string;
    style?: CSSProperties;
}
interface Props extends IconProps {
    src: string;
    alt?: string;
}
export declare const Icon: React.ForwardRefExoticComponent<Props & React.RefAttributes<HTMLImageElement>>;
export {};
