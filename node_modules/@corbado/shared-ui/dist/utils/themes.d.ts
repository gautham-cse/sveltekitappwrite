import type { CustomThemes } from '@corbado/types';
import { BehaviorSubject } from 'rxjs';
export declare const hasDarkMode: () => boolean;
export declare const handleTheming: (darkMode: 'on' | 'off' | 'auto', customTheme?: string | CustomThemes) => {
    darkModeState: BehaviorSubject<boolean>;
    removeTheme: () => void;
};
export declare enum CorbadoThemes {
    EmeraldFunk = "cb-emerald-funk-theme"
}
