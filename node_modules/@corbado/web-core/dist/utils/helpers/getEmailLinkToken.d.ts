import { Err, Ok } from 'ts-results';
import { InvalidTokenInputError } from '../errors';
export declare const getEmailLinkToken: () => Err<InvalidTokenInputError> | Ok<{
    emailLinkId: string;
    token: string;
}>;
