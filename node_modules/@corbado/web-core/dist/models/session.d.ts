import type { SessionUser } from '@corbado/types';
export declare class ShortSession {
    #private;
    constructor(value: string);
    get value(): string;
    get user(): SessionUser;
    isValidForXMoreSeconds(seconds: number): boolean;
    toString(): string;
}
