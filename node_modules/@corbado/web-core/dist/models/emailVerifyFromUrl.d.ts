import type { GeneralBlockVerifyIdentifier } from '../api';
import { AuthType } from '../api';
import { AuthProcess } from './authProcess';
export declare class EmailVerifyFromUrl {
    data: GeneralBlockVerifyIdentifier;
    token: string;
    isNewDevice: boolean;
    process: AuthProcess;
    authType: AuthType;
    constructor(data: GeneralBlockVerifyIdentifier, token: string, isNewDevice: boolean, process: AuthProcess, authType: AuthType);
    static fromURL(encodedProcess: string, token: string, existingProcess: AuthProcess | undefined): EmailVerifyFromUrl;
}
